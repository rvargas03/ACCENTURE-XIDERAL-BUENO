PREGUNTAS SEMANA 3 
ROBERT MOISES VARGAS MAVAREZ

1.
No compila porque estás intentando guardar un int en un String sin convertirlo. Java no hace esa conversión automática.

2.
C, E y F fallan porque usan mal tipos o no definen tamaño del arreglo. En arrays multidimensionales, mínimo la primera dimensión sí o sí debe tener tamaño.

3.
A, C y D funcionan. Java ajusta bien el horario de verano, pero fechas inválidas como “marzo 40” o años no bisiestos truenan.

4.
A, C y D porque equals() compara contenido y == referencias. intern() hace que apunten al mismo objeto del pool.

5.
Resultado B. Todo es encadenado: vas modificando el mismo StringBuilder, no creando nuevos.

6.
Son 2 errores. Math.round(double) regresa long y Math.random() regresa double, no float.

7.
A y E. Convierte todo a GMT y ves que uno está 6 horas antes.

8.
A, B y F dan 5. Ojo con índices base 0 y cómo replace() cambia el string antes de hacer charAt().

9.
A, C y F. Arrays empiezan en 0, son de tamaño fijo y equals() compara referencia, no contenido.

10.
0 errores. Todo compila bien, solo hay que conocer qué tipo regresa cada método.

11.
No compila. LocalDate no maneja horas, entonces plusHours() no existe.

12.
A, D y E. indent y stripLeading se cancelan. Luego puro manejo de índices en substring.

13.
B. String es inmutable (no cambia), pero StringBuilder sí se modifica.

14.
A y F. Solo puedes crear un Instant directamente o desde algo con zona (como ZonedDateTime).

15.
C y E. Números primero, luego mayúsculas, luego minúsculas. Y binarySearch regresa negativo si no encuentra.

16.
A, B y G. Todo gira en cómo cuentan los caracteres de escape y lo que agrega indent.

17.
A y G. substring incluye inicio y excluye fin. Si inviertes índices → excepción.

18.
C y F. String no cambia con métodos, pero sí con +=. Y equals() compara contenido, no memoria.

19.
A, B y D. compare y mismatch dependen del orden y la posición donde difieren.

20.
A y D. Al adelantar horario, se brinca de 2 a 3, pero sigue siendo solo 1 hora de diferencia.

21.
A y C. reverse() es directo, y la otra opción encadena operaciones hasta llegar a "avaJ".

22.
A. LocalDate es inmutable, como no guardas los cambios, se queda igual.


CAPITULO 5

1.
A y E. final puede usarse en variables estáticas o normales, y si es primitiva ya no puedes cambiar su valor.

2.
B y C. Antes del void solo van modificadores válidos como final o private.

3.
A y D. Los modificadores pueden ir en distinto orden mientras la sintaxis siga correcta.

4.
A, B, C y E. El número 6 puede promocionarse a varios tipos primitivos y también convertirse a Integer.

5.
A, C y D. Los métodos void pueden tener return; vacío y los int sí deben regresar un entero válido.

6.
A, B y F. En varargs (...) el parámetro variable siempre debe ir al final y solo puede haber uno.

7.
D y F. En ambos casos el arreglo varargs termina teniendo tamaño 2.

8.
D. Lo más común es usar atributos privados y métodos públicos para controlar acceso.

9.
B, C, D y F. Como están en paquetes distintos, no pueden acceder a miembros private, package-private o protected sin herencia.

10.
B. El bloque static cambia LENGTH a 10 antes de ejecutar main.

11.
B y E. Un método estático no puede llamar directamente uno de instancia sin objeto.

12.
B. Solo giraffe y el primer name son effectively final porque nunca cambian después de asignarse.

13.
D. length es estático, entonces ambos objetos comparten la misma variable.

14.
E. Hay 4 errores porque variables final static solo pueden asignarse una vez.

15.
B. Para importar métodos estáticos se usa import static.

16.
E. short se promociona a int y boolean/double terminan usando Object.

17.
B. Java es pass-by-value, entonces cambiar x dentro del método no modifica value.

18.
B, D y E. Reasignar referencias no afecta afuera, pero modificar el objeto sí.

19.
B, C y E. Variables estáticas pueden modificarse desde bloques estáticos o de instancia.

20.
A y E. Java siempre busca la sobrecarga más específica antes de usar varargs o Object.

21.
B y D. Para sobrecargar métodos deben cambiar los parámetros, no solo el retorno.